[project]
name = "my-email-scanner-dlai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "ollama>=0.6.1",
    "openai>=2.30.0",
    "python-dotenv>=1.2.2",
]
